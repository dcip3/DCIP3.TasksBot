services:
  tasksbot:
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    ports:
      - "${PREVIEW_UPLOAD_PORT:-8081}:${PREVIEW_UPLOAD_PORT:-8081}"
    restart: unless-stopped
